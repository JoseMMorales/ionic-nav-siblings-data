import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ion-nav-project',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
